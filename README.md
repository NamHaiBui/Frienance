# Frienance

See `ARCHITECTURE.md` for the system architecture, launch instructions, and tuning tips.

Quickstart:

- Install dependencies: `flutter pub get`
- Run app: `flutter run`
- Run batch prework: `dart run lib/services/runner.dart`

Environment variables:

- `GEMINI_API_KEY` — required if you use `GeminiService` for LLM-based extraction.

For details, see `ARCHITECTURE.md`.
